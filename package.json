{
  "name": "crowdin",
  "version": "1.0.11",
  "description": "Crowdin is a cloud-based solution that streamlines localization management for your team. It's the perfect place to effectively manage all of your multilingual content.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive -t 10000 -r dotenv/config",
    "deploy": "zapier push",
    "env": "zapier env:set 1.0.5 ${npm_config_var}=${npm_config_val}"
  },
  "engines": {
    "node": ">=v14",
    "npm": ">=5.6.0"
  },
  "dependencies": {
    "@crowdin/crowdin-api-client": "^1.18.1",
    "axios": "^0.27.2",
    "zapier-platform-core": "12.0.3"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "should": "^13.2.3"
  },
  "private": true,
  "zapier": {
    "convertedByCLIVersion": "12.0.2"
  }
}
