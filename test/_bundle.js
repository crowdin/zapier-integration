const bundle = {
    authData: {
        access_token: process.env.TEST_TOKEN || "",
        domain: process.env.TEST_DOMAIN || "serhiy",
    },

    inputData: {},
};

module.exports = {
    bundle
}